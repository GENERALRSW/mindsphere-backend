# railway_api/requirements.txt

fastapi==0.104.1
uvicorn[standard]==0.24.0
motor==3.3.2
pydantic==2.5.0
httpx==0.25.1
python-multipart==0.0.6
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
pymongo==4.6.0
python-dotenv==1.0.0
```

### **2. `Procfile`** (in root directory)
```
web: cd railway_api && uvicorn main_modular:app --host 0.0.0.0 --port $PORT
```

### **3. `runtime.txt`** (in root directory)
```
python-3.11.6
```

---

## 🚀 **Quick Fix - Choose One:**

### **Option A: NIXPACKS (Recommended - Simpler)**

1. **Delete** `railway.toml`
2. **Delete** `Dockerfile`
3. **Create** `Procfile` in root:
```
   web: cd railway_api && uvicorn main_modular:app --host 0.0.0.0 --port $PORT
```
4. **Create** `runtime.txt` in root:
```
   python-3.11.6

